"use client"

import { CompareBar } from "./compare-bar"

/**
 * Client-side wrapper for CompareBar
 * Used in server component layouts
 */
export function CompareBarWrapper() {
  return <CompareBar />
}
